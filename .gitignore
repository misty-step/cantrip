/target
*.wav
!samples/*.wav
!samples/eval/*.wav
/eval/results*/
.env*
/.cargo/config.toml
__pycache__/

/.scratch

/.omp

# Iron Forest workspace (local ledger, not source)
.forest/
/forest
/forest.next
/forest.prev

# Iron Forest renders its agent declaration here per run; opencode installs
# its provider packages here too. Neither belongs in this repository.
.opencode/agents/
.opencode/node_modules/

# Release build output; release plans and public notes remain tracked.
/dist/
/.landmark/run/
