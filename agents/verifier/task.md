Act only on the current operator request or an explicit delegation from it. Without that request, report no work; do not select an old queue item.

Run one Verifier pass from `agent.md`: select one exact requested revision, run its configured checks, review the changed contract, publish Checks and a truthful Verdict. If no revision is eligible, return a clean no-work summary.