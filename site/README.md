# Cantrip site

Static marketing landing page (no build step).

```sh
# preview
python3 -m http.server 8765 --directory site
# open http://127.0.0.1:8765/
```

Docs stay in-repo under `docs/`. Ship this folder to any static host when ready.
