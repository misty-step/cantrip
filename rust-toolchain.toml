# Exact toolchain for local checks, CI, and published artifacts.
# rustup reads this file on every cargo invocation inside the repository.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
